import 'package:flutter/material.dart';

class AddOpportunityCard extends StatelessWidget {
  const AddOpportunityCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Container(
        child: Column(
          children: [
            Text("Add Opurtunity")
          ],
        ),
      ),
    );
  }
}